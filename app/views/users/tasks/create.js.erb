$("#entryform").html("<%= escape_javascript(render :partial => 'form') %>");
$('#tasks-count').text('<%= current_user.tasks.count %>');

<% unless @task.errors.any? %>
  $('#task_name').val('');
<% end %>
